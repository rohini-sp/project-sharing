# Objectives

- Rebuild the XO landing page — cleaner, modern, on-brand with https://xo.builders.
- Pull real messaging/value-props from xo.builders + docs.xo.builders (record in `content/copy.md`).
- Each section: its own partial in `sections/`, styled in `styles.css`, wired into `index.html`.
- Keep the page rendering at all times (open `index.html` to sanity-check before pushing).
