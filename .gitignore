__pycache__/
*.pyc
.venv/
.pytest_cache/

# Machine-written project state stays per-workspace (NOT shared). Only code +
# docs sync via the repo; each workspace keeps its own .xo/ (sessions, memory,
# state, the watcher's commit_relay.json) — avoids constant .xo merge conflicts.
.xo/
